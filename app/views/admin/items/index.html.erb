<%= render partial: "admin/shared/index",
           locals:{resources: @items,
                   action: 'items',
                   path_new: new_admin_item_path,
                   path: "/admin/items/",
                   edition: true,
                   title: 'Items',
                   titles: %w(Title Price Image Avatar),
                   values: %w(title price image_url avatar_attached?),
                   button_new: "New Item"}%>
