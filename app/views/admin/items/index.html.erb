<%= render partial: "admin/shared/index",
           locals:{resources: @items,
                   action: 'items',
                   path_new: new_admin_item_path,
                   path: "/admin/items/",
                   edition: true,
                   title: 'Items',
                   titles: %w(Title Price Avatar),
                   values: %w(title price avatar_attached?),
                   button_new: "New Item"}%>
